#!/bin/bash
magic_content='0 string SCHOOL'
echo "$magic_content" > school.magic
file -C -m school.magic > /dev/null 2>&1
if [ $? -eq 0 ]; then
  echo "Magic file created and compiled successfully."
else
  echo "Failed to compile the magic file."
fi
